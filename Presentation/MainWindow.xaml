<Window x:Class="Presentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModel="clr-namespace:Presentation.ViewModel"
        xmlns:validationRules="clr-namespace:Presentation.ViewModel.ValidationRules"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <viewModel:PoolViewModel/>
    </Window.DataContext>
    <Grid>
        <Button Content="Button" Command="{Binding Path=CreateBallsCommand}" CommandParameter="{Binding ElementName=SpawnAmountTextBox, Path=Text}" HorizontalAlignment="Left" Height="45" Margin="207,217,0,0" VerticalAlignment="Top" Width="172"/>
        <TextBox Name="SpawnAmountTextBox" Text="{Binding Path=SpawnAmountText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Left" Height="45" Margin="452,217,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="141"/>
    </Grid>
</Window>
